# frog-jump

A real-time multiplayer browser game. Express serves the static frontend; Socket.io syncs each connected player's frog state live.

## Running it

```bash
npm install
npm start   # node index.js
```

Then open `http://localhost:3000` (or whatever port Express binds to).
